# 🔍 Face Detection Chatbot

**CCTV/단체 사진에서 얼굴을 감지하고, 성별과 연령대를 분석하여 자연어로 질의응답하는 시스템**

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.109+-green.svg)](https://fastapi.tiangolo.com)
[![React](https://img.shields.io/badge/React-18+-61DAFB.svg)](https://react.dev)
[![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o--mini-412991.svg)](https://openai.com)

---

## 📋 목차

- [개요](#-개요)
- [아키텍처](#-아키텍처)
- [기술 스택](#-기술-스택)
- [설치 방법](#-설치-방법)
- [사용법](#-사용법)
- [API 문서](#-api-문서)
- [프로젝트 구조](#-프로젝트-구조)

---

## 🎯 개요

이 프로젝트는 이미지 분석과 LLM을 결합한 대화형 시스템입니다.

### 핵심 원칙

❌ **LLM이 이미지를 직접 보고 카운팅** (불안정, Hallucination 위험)

✅ **비전 파이프라인 → JSON → LLM이 JSON 기반 답변** (정확도 100%)

### 주요 기능

- 📷 **얼굴 감지**: YOLOv8 기반 실시간 얼굴 검출
- 👫 **성별 분류**: ViT 기반 남/여 분류
- 🎂 **연령대 추정**: 10대/20대/30대/40대+ 분류
- 💬 **자연어 질의**: GPT-4o-mini 기반 대화형 인터페이스

---

## 🏗 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React + Vite)                   │
│        이미지 업로드 │ 분석 결과 시각화 │ 챗봇 UI            │
└───────────────────────────┬─────────────────────────────────┘
                            │ REST API
┌───────────────────────────▼─────────────────────────────────┐
│                    Backend (FastAPI)                         │
│  /upload → /analyze → /result → /chat                        │
│                            │                                 │
│  ┌─────────────────────────┴─────────────────────────────┐  │
│  │                    Storage (SQLite)                    │  │
│  │    analyses 테이블 │ chat_logs 테이블 │ uploads/      │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│              Image Pipeline (Computer Vision)                │
│  YOLOv8 Face Detection → Gender/Age Classification          │
│                            ↓                                 │
│                    JSON Output (Single Source of Truth)      │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                 LLM Module (GPT-4o-mini)                     │
│  JSON + Question → Natural Language Answer                   │
│  ⚠️ Hallucination 방지: JSON 데이터만 사용하여 답변          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠 기술 스택

### Backend
| 기술 | 용도 |
|------|------|
| **FastAPI** | REST API 서버 |
| **SQLAlchemy** | ORM (SQLite) |
| **OpenAI API** | GPT-4o-mini LLM |
| **Uvicorn** | ASGI 서버 |

### Computer Vision
| 기술 | 용도 |
|------|------|
| **YOLOv8** | 얼굴 감지 |
| **ViT (Vision Transformer)** | 성별/나이 분류 |
| **Hugging Face Transformers** | 모델 로드 |

### Frontend
| 기술 | 용도 |
|------|------|
| **React 18** | UI 프레임워크 |
| **Vite** | 빌드 도구 |
| **Vanilla CSS** | 스타일링 |

---

## 📦 설치 방법

### 1. 저장소 클론
```bash
git clone <repository-url>
cd MLPA_face_detection_chatbot
```

### 2. Python 의존성 설치
```bash
# Conda 환경 활성화 (권장)
conda activate faceChat_hdd

# 패키지 설치
pip install -r requirements.txt
```

### 3. 환경변수 설정
```bash
cp .env.example .env

# .env 파일 수정
nano .env
```

**필수 설정:**
```
OPENAI_API_KEY=your-openai-api-key-here
```

### 4. 프론트엔드 설치
```bash
cd frontend
npm install
```

---

## 🚀 사용법

### 서버 실행

**터미널 1: 백엔드 서버**
```bash
cd MLPA_face_detection_chatbot
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

**터미널 2: 프론트엔드 개발 서버**
```bash
cd frontend
npm run dev
```

### 접속
- **프론트엔드**: http://localhost:5173
- **백엔드 API 문서**: http://localhost:8000/docs

### 워크플로우

1. 📷 **이미지 업로드**: 드래그앤드롭으로 이미지 업로드
2. 🔍 **분석 시작**: 자동으로 얼굴 감지 및 분류 실행
3. 📊 **결과 확인**: 성별/연령대 통계 시각화
4. 💬 **질문하기**: 자연어로 결과에 대해 질문

### 예시 질문
- "남녀 비율은 어떻게 되나요?"
- "20대가 몇 명인가요?"
- "가장 많은 연령대는?"
- "분석 결과를 요약해주세요"

---

## 📡 API 문서

### 엔드포인트 목록

| Method | Endpoint | 설명 |
|--------|----------|------|
| `POST` | `/api/upload` | 이미지 업로드 |
| `POST` | `/api/analyze/{analysis_id}` | 분석 시작 |
| `GET` | `/api/result/{analysis_id}` | 분석 결과 조회 |
| `POST` | `/api/chat` | LLM 질의응답 |
| `GET` | `/api/history/{analysis_id}` | 채팅 히스토리 조회 |

### 상세 API

#### 이미지 업로드
```http
POST /api/upload
Content-Type: multipart/form-data

files: [image1.jpg, image2.jpg, ...]
```

**Response:**
```json
{
  "analysis_id": "uuid-string",
  "image_count": 2,
  "message": "Successfully uploaded 2 images."
}
```

#### 분석 결과
```http
GET /api/result/{analysis_id}
```

**Response:**
```json
{
  "analysis_id": "uuid-string",
  "status": "completed",
  "result": {
    "total_faces": 28,
    "gender": {
      "male": 23,
      "female": 5
    },
    "age_group": {
      "10s": 6,
      "20s": 22,
      "30s": 0,
      "40_plus": 0
    }
  }
}
```

#### 채팅
```http
POST /api/chat
Content-Type: application/json

{
  "analysis_id": "uuid-string",
  "question": "남녀 비율은?"
}
```

**Response:**
```json
{
  "analysis_id": "uuid-string",
  "question": "남녀 비율은?",
  "answer": "분석 결과에 따르면 총 28명 중 남성이 23명(82.1%), 여성이 5명(17.9%)입니다."
}
```

---

## 📁 프로젝트 구조

```
MLPA_face_detection_chatbot/
├── README.md                 # 프로젝트 문서
├── requirements.txt          # Python 의존성
├── .env                      # 환경변수 (gitignore)
├── .env.example              # 환경변수 샘플
│
├── backend/                  # FastAPI 백엔드
│   ├── main.py               # 앱 엔트리포인트
│   ├── config.py             # 설정 관리
│   ├── database.py           # SQLite 모델
│   ├── routers/
│   │   ├── upload.py         # 이미지 업로드 API
│   │   ├── analyze.py        # 분석 실행 API
│   │   └── chat.py           # 챗봇 API
│   ├── services/
│   │   ├── llm_service.py    # OpenAI GPT-4o-mini
│   │   └── pipeline_service.py # 이미지 파이프라인 래퍼
│   └── schemas/
│       └── models.py         # Pydantic 스키마
│
├── frontend/                 # React 프론트엔드
│   ├── package.json
│   ├── vite.config.js
│   └── src/
│       ├── App.jsx           # 메인 앱
│       ├── api/
│       │   └── client.js     # API 클라이언트
│       └── components/
│           ├── ImageUploader.jsx
│           ├── AnalysisResult.jsx
│           └── ChatInterface.jsx
│
├── image_pipeline/           # 컴퓨터 비전 파이프라인
│   ├── image_pipeline.py     # 메인 파이프라인
│   └── results/              # 분석 결과 저장
│
├── storage/                  # 데이터 저장소
│   ├── database.db           # SQLite DB
│   └── uploads/              # 업로드된 이미지
│
└── data/                     # 테스트 데이터
    └── classroom/            # 샘플 이미지
```

---

## 📝 라이선스

MLPA 프로젝트 © 2024

---

## 👤 개발자

MLPA Face Detection Chatbot Team